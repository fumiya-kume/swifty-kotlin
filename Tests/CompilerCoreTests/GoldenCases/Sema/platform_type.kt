package golden.sema

// Placeholder: true platform type (T!) requires Java interop stub.
// This case keeps the registry-backed golden asset in sync with the
// integration coverage for imported platform-like metadata.
fun usePlatformType(v: String) {
    val len: Int = v.length
    println(len)
}
